<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:minWidth="280dp"
        android:minHeight="24dp"
        android:updatePeriodMillis="1800000"
        android:previewImage="@drawable/example_appwidget_preview"
        android:initialLayout="@layout/ip_widget_provider"
        android:resizeMode="horizontal"
        android:widgetCategory="home_screen"
        android:initialKeyguardLayout="@layout/ip_widget_provider">
    <!--TODO: See minResizeWidth and minResizeHeight to allow resizing below minWidth and height-->
    <!--TODO: Set updatePeriodMillis to zero and use AlarmManager: Use ELAPSED_REALTIME or RTC.-->
    <!--TODO: This will provide: Smaller update period, no update when de device is locked-->
</appwidget-provider>